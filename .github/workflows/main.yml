name: Run Regresion Tests

on:
  pull_request:
    branches: [ "main" ]
    
jobs:
  run-tests:
    uses: AbyssalWhale/python-pw/.github/workflows/reusable-workflow.yml@AbyssalWhale-GH-Actions
    permissions:
        contents: read
        issues: read
        checks: write
        pull-requests: write
